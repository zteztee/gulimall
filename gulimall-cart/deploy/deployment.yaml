apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gulimall-cart
  name: gulimall-cart
  namespace: gulimall
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: gulimall-cart
  template:
    metadata:
      labels:
        app: gulimall-cart
    spec:
      containers:
        - name: gulimall-cart
          image: docker.io/zte020415/gulimall-cart:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: 500m
              memory: 500Mi
            requests:
              cpu: 100m
              memory: 100Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst

---
kind: Service
apiVersion: v1
metadata:
  name: gulimall-cart
  namespace: gulimall
  labels:
    app: gulimall-cart
spec:
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 8080
      nodePort: 31002
  selector:
    app: gulimall-cart
  type: NodePort
  sessionAffinity: None



