apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gulimall-ware
  name: gulimall-ware
  namespace: gulimall
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: gulimall-ware
  template:
    metadata:
      labels:
        app: gulimall-ware
    spec:
      containers:
        - name: gulimall-ware
          image: docker.io/zte020415/gulimall-ware:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: 500m
              memory: 500Mi
            requests:
              cpu: 100m
              memory: 100Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst

---
kind: Service
apiVersion: v1
metadata:
  name: gulimall-ware
  namespace: gulimall
  labels:
    app: gulimall-ware
spec:
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 8080
      nodePort: 31009
  selector:
    app: gulimall-ware
  type: NodePort
  sessionAffinity: None



