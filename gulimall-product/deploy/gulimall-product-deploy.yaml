apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gulimall-product
  name: gulimall-product
  namespace: gulimall
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: gulimall-product
  template:
    metadata:
      labels:
        app: gulimall-product
    spec:
      containers:
        - name: gulimall-product
          image: docker.io/zte020415/gulimall-product:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: 500m
              memory: 500Mi
            requests:
              cpu: 100m
              memory: 100Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst

---
kind: Service
apiVersion: v1
metadata:
  name: gulimall-product
  namespace: gulimall
  labels:
    app: gulimall-product
spec:
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 8080
      nodePort: 31006
  selector:
    app: gulimall-product
  type: NodePort
  sessionAffinity: None



